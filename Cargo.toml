[package]
name = "grig-core"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
sha3 = "0.10"
zeroize = "1"
criterion = "0.4"

[[bench]]
name = "grig_bench"
harness = false

[dev-dependencies]
criterion = { version = "0.4", features = ["html_reports"] }